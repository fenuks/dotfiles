# vim: ft=yaml
extends: default

rules:
  comments:
    min-spaces-from-content: 1
  document-start: disable
  line-length: disable
  quoted-strings:
    required: only-when-needed
    extra-allowed: [$]
